using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputManager : MonoBehaviour
{
    /*PlayerControls controls;
    PlayerControls.PlayerControlActions playerControlActions;

    Vector2 horizontalInput;

    private void Awake()
    {
        //controls = new PlayerControls();
        /playerControlActions = controls.PlayerControl;

        //playerControlActions.Movement.performed += ctx => horizontalInput = ctx.ReadValue<Vector2>;
    }

    private void OnEnable()
    {
        controls.Enable();
    }

    private void OnDestroy()
    {
        controls.Disable();
    }*/
}
